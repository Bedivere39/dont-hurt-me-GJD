# Frontend env example
# Copy to frontend/.env (Vite auto-loads it at dev/build time)
# In production, VITE_API_BASE is injected by CI (.github/workflows/deploy.yml)

VITE_API_BASE=http://localhost:8000/api
