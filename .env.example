# Project root .env.example - documents all env vars used by backend (and possibly CI)

# Database
DATABASE_URL=postgresql://etf_user:CHANGE_ME@localhost:5432/etf_db

# Flask
SECRET_KEY=change-me-in-production

# Data fetching (Phase 2 will use)
FETCH_ENABLED=false
FETCH_SSE_DAYS=5

# Migration (one-time use)
SOURCE_SQLITE_PATH=
